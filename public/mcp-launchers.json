{
  "$schema": "./launchers.schema.json",
  "version": 1,
  "launchers": [
    {
      "id": "uvx",
      "label": "Run via uvx (recommended)",
      "description": "No permanent install needed. uvx runs Vaner MCP on demand when uvx is available.",
      "command": "uvx",
      "args": ["--from", "vaner[mcp] @ git+https://github.com/Borgels/vaner.git", "vaner", "mcp"]
    },
    {
      "id": "path",
      "label": "Use installed vaner on PATH",
      "description": "Use an already-installed vaner binary from your shell PATH.",
      "command": "vaner",
      "args": ["mcp"]
    }
  ]
}
